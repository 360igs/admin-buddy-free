# Admin Buddy - Free build (public mirror)

This repository is a **read-only public mirror** of the Admin Buddy free build hosted on the [WordPress.org plugin directory](https://wordpress.org/plugins/admin-buddy/).

It contains the exact files shipped in the WordPress.org-hosted zip, refreshed on every tagged release. No build step is required to read or run this code - every file in this repository is the source that runs on a WordPress install.

## Versions

Each release here matches a version on WordPress.org. The git tag `v1.0.1` corresponds to the `1.0.1` zip on WordPress.org. To install a specific version, download the matching zip from WordPress.org or check out the matching tag.

## What's in the Free build

Eight modules ship enabled (or available to enable) in the free build:

- **White Label** - custom admin colours, logos, custom dashboard pages
- **Colours** - full admin colour customisation (Auto Palette is Pro-only)
- **Login** - branded login page
- **Maintenance** - Coming Soon / Maintenance modes with HMAC bypass tokens
- **SMTP** - custom outgoing-mail settings (provider presets are Pro-only; Free supports any SMTP server via the Custom preset)
- **Snippets** - CSS, JS, HTML snippets with syntax checking and per-snippet enable/disable
- **User Roles** - capability editor + role create/clone/delete
- **Quick Settings** - common WordPress hardening + UI toggles

## Pro build

A separate commercial Pro build extends this Free build with additional modules (Menu Customiser, Custom Pages, Collections, Option Pages, Icon Library, Activity Log, Debug, Bricks Builder integration, Remote, Export/Import, Demo Data, Blueprints).

Pro source is **not published here** - only the licensed Pro distribution is available to customers.

## Reporting issues

For bug reports against the free build, please open an issue on this repository or post on the [plugin support forum on WordPress.org](https://wordpress.org/support/plugin/admin-buddy/).

## License

GPL-2.0-or-later. See `admin-buddy.php` for the plugin header and the `LICENSE` file at the repo root for the full license text.
